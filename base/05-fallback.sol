pragma solidity >=0.8.x <0.9.0;


contract Test {
  
  function() payable public {
      
  }
  
// 1. 一个合约可以有且只有一个匿名函数
// 2. 匿名函数没有函数名，没有参数，没有返回值
// 3. 当外界调用一个合约不存在的方法时，会自动调用匿名函数
// 4. 匿名函数一般用于给合约转账，开销更小
    
} 